{
  "name": "kalapaiva",
  "private": true,
  "type": "module",
  "description": "Local dev server only — AnglrLog is static HTML/JS",
  "scripts": {
    "start": "npx --yes serve . -l 5050 -s",
    "test": "node --test tests/*.test.js"
  }
}
