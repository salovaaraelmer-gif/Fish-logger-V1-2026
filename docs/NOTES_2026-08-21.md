# Session notes — 2026-08-21

Context for continuing work. Cursor Agent chats do **not** sync between machines; use this file + git instead.

## Handheld baseline (do not implement firmware)

The AnglrLog Handheld device-logic spec is now the **starting point**:

**`docs/HANDHELD_DEVICE_LOGIC.md`**

Agreed working method:

1. Keep that document as the baseline.
2. Change **document sections** when we decide something.
3. Do **not** ask Cursor to write ESP32 / keypad / OLED firmware yet.
4. Do **not** start Web Bluetooth / Connect Handheld UI until the spec says so.

Phone-side catch **data** prep is already on `main` (`source`, `device_id`, `client_event_id`, `saveHandheldCatch`). That is storage/mapping only. BLE transport is still absent.

A Cursor rule (`.cursor/rules/handheld-no-firmware.mdc`) repeats: no firmware from this spec until the document is updated to allow it. When handheld **building** starts, Cursor must **remind** of the list below before writing code.

## Worth deciding before anything is built

See **§27** in `docs/HANDHELD_DEVICE_LOGIC.md`. Remind of these before firmware or Web Bluetooth:

1. **Prototype GPS** — no NMEA yet, and the phone must not stamp a later GPS fix onto an earlier catch. Null location vs pushing GPS *to* the handheld during the session.
2. **When `caught_at` is taken** — press `1 LOG CATCH` vs after water temperature.
3. **How to skip a displayed NMEA value** — `*` currently means accept.
4. **Resume after disconnect / display sleep** — values stay in memory, UI returns to home.
5. **Web Bluetooth on Android Chrome** — foreground/HTTPS/user-gesture; silent reconnect is not assumed.

## Next

Only edit the handheld spec as decisions land. Continue other AnglrLog app work as usual (mobile nav, etc. — see `docs/NOTES_2026-08-20.md`).
