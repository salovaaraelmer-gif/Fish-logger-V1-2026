# Session notes — 2026-08-20

Context for continuing work on another computer. Cursor Agent chats do **not** sync between machines; use this file + git instead.

## Done today

### Supabase ↔ Cursor
- Linked the repo to Supabase via MCP (`.cursor/mcp.json`), project `jmorifdjtmmmobilhaxm`.
- Agent can apply SQL / migrations from chat when MCP is authenticated.
- Docs/rules updated: prefer MCP; only ask to run SQL in the Dashboard if MCP fails.
- On a new machine: open **Cursor Settings → Tools & MCP**, connect/authenticate **supabase** with the same org.

### UI
- **“Näytä osallistujat”** is hidden on the idle front page (no active session).
- It only shows while a session is active (`css/app.css` + collapse state in `js/app.js`).

## Product decisions (agreed)

### Responsive layouts
- **Auto layout** by screen width (mobile vs desktop).
- Optional **layout preference** later (settings / hamburger menu), not a main toggle.

### Mobile first, then desktop
1. Finish mobile to liking.
2. Then design desktop browser layout.
3. Still one browser app (no separate native apps for now).

### Mobile bottom bar (left → right)
1. **Feed** — friends’ active + past sessions (new; needs friends model later).
2. **Sessio** — active session or start session (boat workflow).
3. **Profiili** — profile tab (not only overlay).
4. **Hamburger / menu** — settings (logout, layout preference, etc.).

### Feed (not built yet)
- No friends/follows tables in Supabase yet.
- Sessions are private today (host + participants only).
- Suggested build order:
  1. **Phase 1** — bottom bar + tab panels; Feed = placeholder.
  2. **Phase 2** — decide friends model (follow vs mutual vs “fished together”) + privacy.
  3. **Phase 3** — Supabase tables + RLS for feed.
  4. **Phase 4** — Feed UI.
  5. **Phase 5** — settings + layout preference.
  6. **Phase 6** — desktop layout.

### Friends model (open decision)
Prefer starting with **Follow** (search username → follow) unless we choose mutual friends or auto “shared a session” later.

### History on Session tab
For Phase 1, keep session history on the Sessio tab; move later if Feed owns past sessions.

## Next step when continuing
Start **Phase 1**: mobile bottom navigation + tab switching (Feed placeholder, Sessio = current home session UI, Profiili, menu sheet).

## Working on two computers
1. `git pull` on the other PC.
2. Same Cursor account; enable **Settings Sync** if you want settings/keybindings synced.
3. Re-authenticate Supabase MCP once on that machine.
4. Chats stay local — continue from this notes file + code.
